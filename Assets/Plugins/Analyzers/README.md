# Unity Roslyn Analyzers

`Microsoft.Unity.Analyzers.dll` was extracted from the NuGet package:

```text
Microsoft.Unity.Analyzers 1.26.0
```

Unity loads this DLL as a Roslyn analyzer because its `.meta` file has the `RoslynAnalyzer` asset label. The plugin is not intended to be included in player builds.

Source package:

```text
https://www.nuget.org/packages/Microsoft.Unity.Analyzers/1.26.0
```

Downloaded package SHA-256:

```text
e1ef8489bd491bfa8f0ddc0a53c6deb3e117e87ade1a387020aa42189cee3e8f
```
